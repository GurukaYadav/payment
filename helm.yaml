appName: payment
service:
  type: ClusterIP
  port: 8080
ingress:
  enabled: false
configMap:
  enabled: true
  data:
    CART_HOST: cart
    CART_PORT: "8080"
    USER_HOST: user
    USER_PORT: "8080"
    AMQP_HOST: rabbitmq-prod.roboshop.internal
    AMQP_USER: roboshop
externalSecret:
  enabled: true

