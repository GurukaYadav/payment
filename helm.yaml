appName: payment
service:
  type: ClusterIP
  port: 8080
ingress:
  enabled: false
configMap:
  enabled: true
  data:
    CART_HOST: cart
    USER_HOST: user
    AMQP_HOST: rabbitmq-prod.roboshop.internal
    AMQP_USER: roboshop
    AMQP_PASS: roboshop123

